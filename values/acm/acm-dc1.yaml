# Hub-only values for dc1, layered under values/clusters/acm-dc1.yaml.
#
# Reserved for elements that manage the hub's own control plane - advancedClusterManagement and
# openshiftGitops. Both are deliberately left disabled: this cluster's ACM and ArgoCD are what
# deploy PolicyStack, so having PolicyStack reconcile them means a bad value can break the delivery
# path that would otherwise fix it. Enable them here once the rest of the stack is proven.
#stack:
